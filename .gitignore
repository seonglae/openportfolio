node_modules/

# Build output
browser/dist/
browser/.vite-temp/

# Secrets and machine-local config. `.env.example` is the tracked template.
.env.local
.env*.local
browser/.env.local
browser/.env.production

# Convex codegen for the browser bundle
browser/convex/

# Manual-adapter holdings files are personal position data, not fixtures.
holdings*.json

# Agent and tooling scratch
tmp/
.codex-home/
browser/dist-demo/
browser/.vite-temp-demo/
site/sitesrv.pid

# iOS: the signing config, the generated project, and the generated plist.
ios/**/Secrets.xcconfig
ios/**/*.xcodeproj/
ios/**/Info.plist
ios/**/build/
